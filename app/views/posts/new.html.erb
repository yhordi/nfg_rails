<%= form_for @post do |f| %>
  Post Title: <%= f.text_field :title, placeholder: "title" %>
  Author: <%= session[:username]%>
  <br></br>
  Post Body: <%= f.text_area :body, :cols => "60", :rows => "10", placeholder: "body" %>
  <%= f.submit "create" %>
<% end %>
